# 测试与性能报告

日期：2026-09-14。当前版本：Wildwise 0.3.0。结论：源码、工程检查、隔离独立专服和无画面原版 API 链路可运行；**尚未达到完整工坊发版门槛**。最新维护回归见下节；此前的配置、性能与移除测试保留为历史证据。

## 0.3.0 能力与皮肤适配验证

本轮：**121 项 Lua 回归、7 项采集器夹具、111 项原版引擎／无画面界面合约通过**；52 个 Lua 文件静态检查 0 warning／0 error。完整结果见 [落实记录](implementation-2026-09-14.md)、[原始标记](evidence/capability-results.txt)及[结构化汇总](evidence/capability-status.json)。下方 0.2.1 及更早数据均保留为历史快照。

111 项包含原有 85 项同步／界面回归、本轮 17 项能力检查、6 项外观生命周期和 3 项异步落地。实际耕地通过原版动作创建土堆并消耗工具次数；肉架通过原版容器转移及 STORE 挂入材料。外观测试核对原版图像数据、缩放、父子移动与烧毁清理；皮肤传参使用资产替身，**不验证皮肤贴图渲染**。真实 HUD 换肤／Combined Status、远程操作和 Caves 往返仍未验收。

本轮重新运行 100／500／1000 件掉落探针，每组 20 秒、200 个世界 Tick 样本，均数量守恒、待处理归零；p95 分别 **0.1970／2.1230／2.1440 ms**，p99 为 **2.1750／2.1520／2.3100 ms**。只计 Wildwise Tick CPU 时间，单个原版调用不能抢占，不能解释为整体帧率或六人预算达标。

新增实机步骤见 MAN-030 系列，均保持待执行。正常生产启动仍不加载 UI；专服中的 Widget 由测试脚本显式创建。假身份请求的 Invalid RPC sender list 不代表远程通信成功，FROMNUM 日志也不代表图像已渲染。

## 第二轮补采与修复验证（0.2.1 历史）

公开接口覆盖 **35 个 DST 模组、58 页、4,584 条去重评论**，其中 **3,588 条**在北京时间 2023-09-14～2026-09-14 内；均到达时间边界或末页。展开为 **74 个可执行问题场景**，引用 101 条精确评论及讨论回复；不是 74 个已复现的独立根因。见[完整案例](workshop-issues.md)和[采集清单](evidence/workshop-coverage.json)。

| 检查 | 当前结果 | 范围 |
|---|---|---|
| 格式、静态分析、Lua 编译、打包白名单 | 48 个 Lua 文件，0 warning / 0 error | 固定 Lua 5.1 与 StyLua，`npm run check` |
| 纯 Lua 回归 | **99 通过，0 失败** | 首轮 68 项＋本轮 31 项；`tests/workshop_spec.lua` |
| 采集器夹具 | **7 通过** | 精确日期、重复 ID、错误 offset、截断、重复整页、非 DST 排除；不联网 |
| 新增真实引擎 | **34 通过** | 23 个具体 prefab、3 种再生植物、料理、最爱食物、包裹、马蹄铁、回血、JSON 预算、旅行取消、Woby 晾肉架 |
| 本轮复跑的既有真实引擎 | **51 通过** | 26 实体＋9 回归＋7 UI＋8 容器＋1 主机通信；主机通信现含两种锅的查询往返 |
| 首轮保留的专项结果 | **4 通过，未因本轮重复运行** | 2 异步落地＋2 真实读档／解包；下文保留当时设置与范围 |
| 图形／真实多人／手柄 | **29 项人工用例待执行** | 见 [manual-regressions.md](manual-regressions.md)，不能用生成角色替代远程玩家 |

第二轮最初 18 项回归为 **69 通过／17 失败**；修复后 86 项全过。后续独立加入位面字段、查看者读取隔离、口袋存储和骑乘动作回归，再暴露 4 个失败断言，分别修复。最终加入最高血量刷新、Woby、RPC 类型、过期料理响应和两种锅查询后为 99 项全过。失败断言数量不等于独立缺陷数量。

新增引擎测试首次出现 3 项失败：弹丸夹具写错原版名称、阿比盖尔出生无敌状态使回血预期不成立、旧进程仍缓存修改前的 Woby 读取器。核对官方脚本修正夹具、重启加载最终代码后 34 项通过；这些夹具／进程问题不算生产修复。原始测试标记与采集器结果见 [workshop-results.txt](evidence/workshop-results.txt)。

本轮生产改动包括：pickable 与 Woby 计时；位面字段；公共／个人读取器分别隔离；字段类型、UTF-8 与总文本预算；发送失败重试；熟肉别名、输入／配方校验及判定隔离；普通锅／便携锅选择与过期响应丢弃；容器和包裹有界定位；制作焦点、骑乘动作与旅行取消。中文注释、函数空行和 README 同步更新。

所有引擎检查仍基于官方 public **24700372 / 游戏 747465**。本轮未跑新的整体性能比较；下方集中掉落数据保留其原始范围，不把它宣布成整场游戏或多人性能最优。

## 首轮架构与工坊问题回归（历史结果）

官方 SteamCMD 刷新分支信息后确认 public **24700372** 与已安装版本一致，游戏 **747465**。updatebeta **25265075** 较新但未测试；[版本记录](evidence/current-engine.json)包含 scripts.zip 校验值。基于这份原版脚本检查当前机制，包括新版多格晾肉架、读档入口、原生 placer／动作、库存、骑乘和木牌。

| 检查 | 本轮结果 | 边界 |
|---|---|---|
| StyLua / luacheck / Lua 编译 | 格式通过；45 个 Lua 文件静态检查 0 warning / 0 error | 固定格式与 Lua 5.1；不替代引擎运行 |
| 纯 Lua | **68 通过，0 失败**，此前 52 项＋本轮 16 项 | 首批 7 项在修复前失败，修复后转绿；包括保存／分片、来源／权限、异常隔离、输入／回调、内部模块交互 |
| 原版专服实体 | **26 通过** | 原生组件、食用差值、库存余量、骑乘／顺从计时、MapExplorer |
| 新增原版回归 | **9 通过** | Woby／空挂载、只读角色状态、新晾肉架、寄生暗影删除、双向虫洞、木牌生命周期和图像更新 |
| 原生 UI 合约 | **7 通过** | HUD、菜单、原生配置、按键回调、预览、hoverer 重建清理；无画面 |
| 木牌类型 | **8 通过** | 本轮默认配置下 8 类真实容器；此前覆盖配置测试保留在下节 |
| 异步掉落 | **2 通过** | 真实 lootdropper 等待落地合堆、原生 DropItem 来源保护 |
| 主机通信合约 | **1 通过** | 本地主机夹具握手／偏好／旧会话；没有连接真实远程玩家 |
| 保存恢复与解包 | **2 通过** | 真实重启并开启 stack_loaded 后，981 个恢复地面物被正确分类；原版包裹解开的一件燧石未误标为读档物 |

原始标记输出见 [maintenance-results.txt](evidence/maintenance-results.txt)。新增引擎测试最初暴露了夹具前提错误：无画面生成的 Wilson 没有本地动作选择组件，且 GetParent 返回 EntityScript 而不是 `.entity`；改为挂原生组件和核对原生父对象后通过。这些不算生产修复。原版 minisign 的 FROMNUM 日志仍按下文边界处理。

最终核对发现原版解包同样调用 SetPersistData，因此读档分类还必须检查调用时的 `POPULATING`。先扩展 WW-I01，得到 67 通过／1 失败，再修复至 68 通过；随后保存重启真实专服执行 `engine_restore.lua` 的 WW-I04／I05，验证恢复分类和解包边界。此阶段只在隔离配置中开启 `items_stack_loaded`，不改发布默认值。

本轮负载探针在异步掉落测试结束后单独执行，每组 20 秒、200 个组件 Tick 样本；没有并行运行其他测试：

| 掉落数 | 数量守恒 | 最终待处理 | p50 ms | p95 ms | p99 ms |
|---|---|---|---|---|---|
| 100 | 是 | 0 | 0.0170 | 0.0760 | 2.1150 |
| 500 | 是 | 0 | 0.0180 | 2.1080 | 2.1650 |
| 1000 | 是 | 0 | 0.0340 | 2.1380 | 2.2130 |

测量只覆盖 `wildwise_world:Tick` 的 CPU 时间，不包括实体生成、全游戏帧时、客户端渲染和网络。2 ms 是候选间让出的预算，单个原版调用不可抢占，故 p95 可以略超预算。此结果没有证明六人常规场景 ≤2 ms，也没有证明优于参考组合或整体最优。

工坊来源与测试对应关系见[问题审查](workshop-issues.md)。另建立 **13 项可执行步骤的人工用例**，见[实机回归](manual-regressions.md)，均未填成通过。真实双向虫洞测试运行于 Master，不能冒充实际 Caves 穿越；原版 Widget 测试不能冒充截图。

## 0.2.1 小木牌类型开关回归

Lua 静态检查 40 个文件无警告、无错误；52 项纯 Lua 测试通过。新增验证覆盖默认注册范围、类型独立开启／关闭、总开关与同功能模组冲突的优先级，以及未知类型不会挂载辅助牌。原生配置与两份 README 的完整示例均包含 38 项。

在隔离专服上执行 `tests/engine_signs_smoke.lua`，生成全部 8 种真实容器并放入适用物品，检查辅助牌实体、父子关系和图标数据。默认配置下，木箱、龙鳞宝箱和远古船货舱有牌，切斯特、哈奇、冰箱、盐盒和鱼类储物箱无牌，8 项通过。再通过 `modoverrides.lua` 关闭木箱、开启默认关闭的 5 种类型，重启后重复测试，8 项通过。6 项无画面原生 UI 测试通过，包括原生配置页包含全部类型开关。原始结果见 [sign-types.txt](evidence/sign-types.txt)；这些结果不验证图形客户端中的贴图外观与摆放位置。

复现默认配置检查：`dofile('../mods/Wildwise/tests/engine_signs_smoke.lua')()`。修改配置并重启后，可传入对应预期覆盖表，例如 `dofile('../mods/Wildwise/tests/engine_signs_smoke.lua')({ treasurechest = false, chester = true, hutch = true, icebox = true, saltbox = true, fish_box = true })`；等待 `[WW_SIGN_RESULT]` 再进行后续操作。

## 0.2.0 配置回归

配置入口、内部模块结构、个人偏好和中英文 README 已同步更新。Lua 静态检查 39 个文件无警告、无错误；48 项纯 Lua 测试通过，包括全部 30 项原生配置的默认值与枚举映射、关闭项、零阈值、非法值回退、模块对象隔离、嵌套偏好恢复、RPC 和 README 示例。

在同一隔离专服上通过原生 `modoverrides.lua` 设置 `beefalo_hunger_threshold = 25`、`info_font_size = 26`、`healthbars_numbers = false`、`map_share_position = "off"`、`items_radius = 6`，确认各值进入相应模块字段。重新执行实体测试 26 项、无画面原生 UI 测试 6 项、主机通信合约 1 项和异步掉落测试 2 项，全部通过。UI 测试包含原生模组配置页的 30 个选项、F7 对嵌套偏好的读写，以及个人修改不污染服务器规则；仍不代表画面与真实鼠标操作验收。原始结果见 [configuration.txt](evidence/configuration.txt)。

## 环境与证据

以下记录为 0.1.0 的初版基线；0.2.0 的新增验证和复测结果见上节。

测试机器原先未安装游戏。测试通过 SteamCMD 匿名下载官方 DST Dedicated Server App 343050，取得游戏版本 **747465**，在单独的临时存储目录创建小型生存世界，绑定回环地址、离线 LAN，仅运行开发测试。测试使用独立存档，未登录游戏账号或安装图形客户端。

环境是 2 vCPU 的 Intel Xeon E5-2680 v4 KVM 虚拟机，内存约 7.26 GiB，Linux 6.8.0-49，Node 24.18.0，独立测试使用 Lua 5.1。完整参数见 [environment.json](evidence/environment.json)。

| 检查层 | 结果 | 能证明什么 |
|---|---|---|
| Lua 静态检查 | 0 warning / 0 error | 语法、未定义变量和常见静态问题 |
| 纯 Lua 自动测试 | 40 项通过 | 缓存、生命周期、权限、来源、守恒、队列、规划、协议与迁移不变量 |
| 原版专服实体测试 | 26 项通过 | 真实 prefab/组件、食用效果、healthdelta、库存余量、MapExplorer、牛骑乘与计时 |
| 原版异步掉落 | 2 项通过 | lootdropper 完成落地后合堆，主动 DropItem 来源受保护 |
| 无画面原生 UI 合约 | 4 项通过 | 真实 HUD 与中英文全部菜单页构造／刷新、原版键位回调、预览 prefab |
| 无画面主机路径合约 | 1 项通过 | 真实 Client 与世界服务的握手、心跳、偏好、旧会话丢弃和关闭；身份／HUD 焦点为夹具 |
| 保存与重启 | 已执行多次 | 隔离专服保存并从同一世界恢复加载 |
| 停用后加载 | 通过基础检查 | 同一存档在 No mods registered 下加载；世界与出生门存在，继续保存 |
| 安装包 | UTF-8、Lua 编译、白名单检查 | 安装目录结构和可复现归档，不包含参考源码与游戏资产 |

专服逐项输出见 [dedicated-results.txt](evidence/dedicated-results.txt)。`server does not load UI` 检查的是正常服务启动未创建客户端；后续 UI 测试是开发控制台显式加载的，不能把此测试流程当作生产专服的 UI 加载行为。

停用模组后的基础加载输出见 [removal.txt](evidence/removal.txt)，完整升级／重启用和玩家资产回归仍待完成。

这些测试未连接任何人类玩家。两个生成的 Wilson 实体不是两名远程联机玩家；真实对象测试也不能替代网络预测、实际输入和画面验收。

## 已通过实测修正的问题

1. `modinfo.lua` 使用了受限环境不存在的迭代函数；改用数值循环后真实专服成功加载。
2. 自然生成的物品还没有完成落地，不能直接视为合堆候选；分别补充受控落地测试和真实异步 lootdropper 测试。
3. 原版 GiveItem 会把部分容量的余量拿到鼠标上；改为按 CanAcceptCount 使用原版 Get 拆分，实测库存 38→40，地面 5→3。
4. 夜间重新加载存档使可见性测试前提变化；使用原版夜视能力固定测试前提，保留生产代码的光照权限检查。
5. 牛顺从修改接口是 DeltaObedience；真实骑乘后验证 `_bucktask` 存在并随顺从提升更新。
6. 集中掉落暴露重复邻居查询成本；改用获准候选的空间索引和数量／CPU 双预算，下面记录同一开发探针的结果。

原版 minisign 在 nullrenderer 中直接调用 OnDrawn 也会记录 `Could not find anim build FROMNUM`。当前测试验证 drawable 数据和实体生命周期，不把该日志当作已验证的贴图渲染结果；小木牌图片仍需图形客户端查看。

## 集中掉落探针

`tests/engine_load.lua` 在真实专服创建 100、500、1000 个已落地的原版 flint，每组运行 20 秒。每组约 200 个 Wildwise 世界 Tick 样本；使用 `os.clock()` 统计此组件的 CPU 耗时。角色未连接，场景不含客户端渲染、生成 prefab 的初始耗时、全游戏帧耗时或网络流量。每组末尾验证数量并移除测试物。100 件组的前六秒同时运行了两件物品的异步掉落测试，位置与负载物品分离；其余两组没有此并行场景。

| 掉落数 | 数量守恒 | 最终待处理 | p50 ms | p95 ms | p99 ms |
|---|---|---|---|---|---|
| 100 | 是 | 0 | 0.017 | 0.156 | 2.116 |
| 500 | 是 | 0 | 0.022 | 2.105 | 2.163 |
| 1000 | 是 | 0 | 0.034 | 2.144 | 2.224 |

原始记录：[item-load.txt](evidence/item-load.txt)。修正前的同类开发探针记录在 [item-load-before.txt](evidence/item-load-before.txt)，用于解释实现热点，**不是与参考模组或原版游戏的性能对照**。空世界、前置验证和世界状态仍可能影响结果，不能直接转成宣传百分比。

CPU 预算只在原版调用之间让出，不能打断单个调用，所以高分位可略超过 2 ms。PRD 的“6 人常规场景新增 Lua p95 ≤ 2 ms”没有在这里得到验证；本报告也不宣称已满足。当前 1000 件场景的 p95 约 2.14 ms 是公开保留的测量结果。

## 复现

开发依赖与 `npm run check` / `npm run package` 见中英文 README。引擎测试必须从源码 checkout 执行，安装包不含 tests。

1. 使用合法取得的 DST 独立专服，安装源码文件夹到 `mods/Wildwise`。
2. 创建隔离测试 cluster，设置离线 LAN、空服不暂停、小型生存世界；启用 Wildwise，不使用私人游戏存档。
3. 从 `bin64` 目录启动专服，世界加载完成后在其控制台逐条运行：

```lua
dofile('../mods/Wildwise/tests/engine_smoke.lua')
dofile('../mods/Wildwise/tests/engine_ui_smoke.lua')
dofile('../mods/Wildwise/tests/engine_workshop.lua')
dofile('../mods/Wildwise/tests/engine_regression.lua')
-- 等待 WW_REG_RESULT，木牌子测试使用异步回调。
dofile('../mods/Wildwise/tests/engine_signs_smoke.lua')()
-- 等待 WW_SIGN_RESULT，再继续。
dofile('../mods/Wildwise/tests/engine_events.lua')
-- 等待 WW_EVENT_RESULT，再开始负载探针，避免混入事件场景。
dofile('../mods/Wildwise/tests/engine_load.lua')
-- 等待 WW_LOAD_COMPLETE；约 60 秒。
dofile('../mods/Wildwise/tests/engine_client_smoke.lua')
```

4. 每条命令单独提交；等对应 RESULT 再继续。实际目录名若为小写 `wildwise`，同步修改命令路径。保留带 `WW_` 标签的结果，保存并关闭专服，再重新启动确认保存可恢复。`c_shutdown(false)` 的参数不是“不保存”标志，不要据其名字推断存档语义。
5. 无画面 UI 测试显式创建并清理真实 Widget；主机合约使用测试身份，不应在公开服务器上运行。

保存恢复专项：先在含地面物品的隔离世界保存退出，将其 `modoverrides.lua` 的 `items_stack_loaded` 设为 true 并重启，待世界加载完成后运行 `dofile('../mods/Wildwise/tests/engine_restore.lua')`，等待 `WW_RESTORE_RESULT`。结束后恢复原测试配置。它检查真实 world population 和原版包裹解包；不能替代 MAN-SAVE01 的完整升级、停用、回档与复杂资产测试。

## 发版前仍需完成

- 本地主机窗口 + 一名远程客户端：实体引用、角色差异、输入接管、动作预测与结果、独立地图共享隔离。
- Master/Caves 实际连接、往返、断线重连、回档与编号恢复；不同共享模式和关闭共享的历史边界。
- 图形客户端截图及交互：不同分辨率／缩放、中文字体、血条遮挡、包裹／皮肤／调味小木牌、牛状态栏、地图投影、船上预览。
- Geometric Placement、Minimap HUD、Combined Status；实际同功能模组停用提示；角色特殊形态。
- 两名玩家竞争拾取、船岸、落石、工具损坏、所有动作类别、全部原版食用修正和库存特例。
- 同环境、同存档的原版／参考组合／Wildwise 对照，6 人与 12 人场景，客户端与服务端 p50/p95/p99、消息量、内存和至少两小时持续运行。
- 升级、移除与重新启用的完整存档回归，以及正式截图与工坊发布检查。

测试缺口来自当前没有图形游戏客户端与真实多人测试环境；因此提供开发版本与明确的后续验收清单，不把静态测试或 nullrenderer 当作联机验收完成。
